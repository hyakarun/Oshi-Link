export const metadata = {
  title: '管理コンソール',
  robots: { index: false, follow: false },
};

export default function ManageHqLayout({ children }: { children: React.ReactNode }) {
  return children;
}
